const START = { path: '/', name: 'Start' }
const CLASSROOM = { path: '/classroom', name: 'Classroom' }
const Login = { path: '/login', name: 'Login' }

const DISABLE_NAVIGATION = [Login.path]

export { START, Login, CLASSROOM }
